# Only for dev/check-schemas. The pins are pspdx-catalog's, so both
# repositories hold files to the schemas with the same validator. The two
# validators are what makes jsonschema check "date-time", "uri" and
# "uri-reference" at all: without them those formats pass unread.
jsonschema==4.26.0
rfc3339-validator==0.1.4
rfc3986-validator==0.1.1
