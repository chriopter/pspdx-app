# PSPDX catalog format

The [format specification](../README.md#format-specification) is maintained
in the project README. Catalogs use this file’s URL as their schema identifier.

The [v1 manifest schema](v1.pspdx) defines individual `.pspdx` files.
